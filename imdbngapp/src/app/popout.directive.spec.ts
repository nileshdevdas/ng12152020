import { PopoutDirective } from './popout.directive';

describe('PopoutDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoutDirective();
    expect(directive).toBeTruthy();
  });
});
